#!/usr/bin/env sh

# This file runs when a DM logs you into a graphical session.
# If you use startx/xinit like a Chad, this file will also be sourced.

remaps &		# run the remaps script, switching caps/esc and more; check it for more info
# setbg &			# set the background with the `setbg` script
# xcompmgr &		# xcompmgr for transparency
# $STATUSBAR &		# script for updating the status bar
dunst &			# dunst for notifications
# if test -n "$WORK"; then
#   sxhkd -m 1 -c "$HOME/.config/sxhkd/sxhkdrc.work" &		# Bind keys with sxhkd
# elif test -n "$HOME_BIG_MONITOR"; then
#   sxhkd -m 1 -c "$HOME/.config/sxhkd/sxhkdrc.home" &		# Bind keys with sxhkd
# elif test -n "$HOME_TWO_MONITOR"; then
#   sxhkd -m 1 -c "$HOME/.config/sxhkd/sxhkdrc.home2" &		# Bind keys with sxhkd
# else
#   sxhkd -m 1 &		# Bind keys with sxhkd
# fi
unclutter --ignore-scrolling --timeout 2 --jitter 15 --exclude-root --fork & 	# Remove mouse when idle
# mpd-module-update &	# Check for when to update the mpd module
# notify-send -i /home/luke/.local/share/larbs/larbs.png "Welcome to LARBS" "Press super+F1 for the help menu." # LARBSWELCOME

libinput-gestures-setup start &
greenclip daemon &
xiccd &
keynav &
